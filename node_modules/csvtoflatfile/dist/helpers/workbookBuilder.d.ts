import type { Action, FlatfileWorkbook } from '../types/Flatfile.js';
import type { FieldType, SheetAccessOptions } from '../types/ParseCsv.js';
type CsvToWorkbookProps = {
    actions?: Action[];
    fieldKeys?: string[];
    fieldTypes?: FieldType[];
    labels: string[];
    sheetName: string;
    records: string[][];
    slugName?: string;
    workbookName: string;
    sheetAccess?: SheetAccessOptions[];
    workbookEnvironmentId?: string;
    workbookSpaceId?: string;
};
export declare const mapCsvToWorkbook: ({ actions, fieldKeys, fieldTypes, labels, records, slugName, sheetName, workbookName, sheetAccess, workbookEnvironmentId, workbookSpaceId, }: CsvToWorkbookProps) => FlatfileWorkbook;
export {};
